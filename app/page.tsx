export const revalidate = 0;

export default async function Home() {
  return (
    <main className="no-scrollbar w-screen h-screen text-6xl flex items-center justify-center">
      Hello
    </main>
  );
}
